apply plugin: 'org.springframework.boot'

description = 'Example Application Client'

dependencies {
    compile project(':application-api')

    compile libs.frostnova.check
    compile libs.springBootApp
    compile libs.springBootJerseyClient

    testCompile libs.testbase
}
